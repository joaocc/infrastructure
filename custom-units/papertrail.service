[Unit]
Description=Log forwarder

[Service]
ExecStart=/bin/sh -c "journalctl -o short -f | ncat --ssl logs2.papertrailapp.com `etcdget /papertrail.com/port`"

[Install]
WantedBy=multi-user.target

[X-Fleet]
Global=true